{
  "name": "haven-v0",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HAVEN — idle visuel : un petit monde qui se transforme.",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test",
    "test:mobile": "node scripts/check-mobile-resume.mjs",
    "simulate": "node scripts/simulate-progression.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "vite": "^5.4.10"
  }
}
