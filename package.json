{
  "name": "haven-v0",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HAVEN — idle visuel : un petit monde qui se transforme.",
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test",
    "simulate": "node scripts/simulate-progression.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
